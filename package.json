{
  "name": "bitte-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bitté 拼贴编辑器后端",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build && cd ../admin && npm install && npm run build",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "pg": "^8.12.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5"
  }
}
