services:
  - type: web
    name: bitte
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: bitte-db
          property: connectionString

databases:
  - name: bitte-db
    plan: free
    databaseName: bitte
