{
  "version": 2,
  "buildCommand": "cd client && npm install --production=false && npm run build && cd ../admin && npm install --production=false && npm run build",
  "outputDirectory": "public",
  "installCommand": "npm install --production=false",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/admin/(.*)", "destination": "/admin/index.html" },
    { "source": "/((?!api|admin).*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.js": {
      "memory": 256,
      "maxDuration": 10
    }
  }
}
